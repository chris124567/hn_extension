{
  "name": "hn-guideline-collapser",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A Chrome extension that collapses Hacker News comments using configurable guideline-classifier scores.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "format": "prettier --write . --ignore-unknown",
    "format:check": "prettier --check . --ignore-unknown",
    "typecheck": "tsc",
    "test": "node --test tests/*.test.mjs",
    "bundle": "esbuild src/content.ts src/background.ts src/options.ts --bundle --format=iife --target=chrome109 --outdir=dist",
    "build": "npm run typecheck && npm run bundle",
    "check": "npm run format:check && npm run typecheck && npm test && npm audit --audit-level=moderate",
    "package": "npm run check && npm run bundle && python3 scripts/package.py"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.6",
    "esbuild": "^0.28.2",
    "prettier": "^3.6.2",
    "typescript": "^7.0.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
